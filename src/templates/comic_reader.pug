extends website.pug

block append title
  | #{page_title_string} | Comics | Kamigen | A Tropical Cyberpunk RPG

block append content
  .row
    .column
      .ui.one.column.stackable.center.aligned.page.grid
        .column.five.wide
          a(href=previous_url)
            i.ui.icon.reply
            | Previous
        .column.six.wide
          a(href="/comics.html")
            i.ui.icon.book
            | Page Gallery
        .column.five.wide
          if next_url
            a(href=next_url)
              i.ui.icon.share
              | Next
          else 
            p Coming soon!
  .row
    .column
      .ui.one.column.stackable.center.aligned.page.grid
        .column.sixteen.wide
        if next_url
          a(href=next_url)
            img.ui.image.fluid(src=image_url)
        else
          a(href="/comics.html")
            img.ui.image.fluid(src=image_url)
  .row
    .column
      .ui.one.column.stackable.center.aligned.page.grid
        .column.five.wide
          a(href=previous_url)
            i.ui.icon.reply
            | Previous
        .column.six.wide
          a(href="/comics.html")
            i.ui.icon.book
            | Page Gallery
        .column.five.wide
          if next_url
            a(href=next_url)
              i.ui.icon.share
              | Next
          else 
            p Coming soon!
